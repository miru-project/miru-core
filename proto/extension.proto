syntax = "proto3";

package miru;

import "proto/extension_model.proto";

option go_package = "github.com/miru-project/miru-core/proto";

// FilterSelectionValue holds the selected option keys for a single filter.
message FilterSelectionValue { repeated string values = 1; }

// FilterSelection is the typed filter selection the frontend sends. It
// replaces the former JSON string so the entire filter pipeline (proto,
// Go runtime, gRPC handlers) is statically typed with zero JSON strings.
message FilterSelection { map<string, FilterSelectionValue> selections = 1; }

message SearchRequest {
  string pkg = 1;
  string kw = 2;
  int32 page = 3;
  FilterSelection filter = 4;
}

message CreateFilterRequest {
  string pkg = 1;
  FilterSelection filter = 2;
}

message CreateFilterResponse {
  map<string, ExtensionFilter> filters = 1;
}

message SearchResponse {
  repeated ExtensionListItem items = 1;
  string raw = 2; // Raw JSON matching JS function return
}

message LatestRequest {
  string pkg = 1;
  int32 page = 2;
}

message LatestResponse {
  repeated ExtensionListItem items = 1;
  string raw = 2; // Raw JSON matching JS function return
}

message DetailRequest {
  string pkg = 1;
  string url = 2;
}

message DetailResponse {
  ExtensionDetail data = 1;
  string raw = 2; // Raw JSON matching JS function return
}

message WatchRequest {
  string pkg = 1;
  string url = 2;
}

message MirrorRequest {
  string pkg = 1;
  string url = 2;
}

message MirrorResponse {
  oneof data {
    ExtensionBangumiWatch bangumi = 1;
    ExtensionMangaWatch manga = 2;
    ExtensionFikushonWatch fikushon = 3;
    ExtensionAllWatch all = 5;
  }
}

message WatchResponse {
  oneof data {
    ExtensionBangumiWatch bangumi = 1;
    ExtensionMangaWatch manga = 2;
    ExtensionFikushonWatch fikushon = 3;
    ExtensionWatch watch = 4;
    ExtensionAllWatch all = 6;
  }
}

// Extension Management
message DownloadExtensionRequest {
  string repo_url = 1;
  string pkg = 2;
}
message DownloadExtensionResponse { string message = 1; }

message RemoveExtensionRequest { string pkg = 2; }
message RemoveExtensionResponse { string message = 1; }

message GetExtensionSettingsRequest { string pkg = 1; }
message GetExtensionSettingsResponse {
  repeated ExtensionSetting settings = 1;
}

message SaveExtensionSettingsRequest {
  string pkg = 1;
  repeated ExtensionSetting settings = 2;
}
message SaveExtensionSettingsResponse { string message = 1; }

service ExtensionService {
  rpc Search(SearchRequest) returns (SearchResponse);
  rpc CreateFilter(CreateFilterRequest) returns (CreateFilterResponse);
  rpc Latest(LatestRequest) returns (LatestResponse);
  rpc Detail(DetailRequest) returns (DetailResponse);
  rpc Watch(WatchRequest) returns (WatchResponse);
  rpc Mirror(MirrorRequest) returns (MirrorResponse);
  rpc DownloadExtension(DownloadExtensionRequest)
      returns (DownloadExtensionResponse);
  rpc RemoveExtension(RemoveExtensionRequest) returns (RemoveExtensionResponse);
  rpc GetExtensionSettings(GetExtensionSettingsRequest)
      returns (GetExtensionSettingsResponse);
  rpc SaveExtensionSettings(SaveExtensionSettingsRequest)
      returns (SaveExtensionSettingsResponse);
}
