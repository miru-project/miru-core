// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/miru-project/miru-core/ent/tracker"
)

// Tracker is the model entity for the Tracker schema.
type Tracker struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// The external tracker ID
	TrackerID string `json:"tracker_id,omitempty"`
	// The provider of the tracker
	Provider tracker.Provider `json:"provider,omitempty"`
	// Status of the tracking
	Status string `json:"status,omitempty"`
	// Score given by the user
	Score *int `json:"score,omitempty"`
	// Progress of the media
	Progress int `json:"progress,omitempty"`
	// Total progress of the media (total episodes/chapters)
	TotalProgress *int `json:"total_progress,omitempty"`
	// Start date in unix timestamp
	StartDate *int64 `json:"start_date,omitempty"`
	// Finish date in unix timestamp
	FinishDate *int64 `json:"finish_date,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TrackerQuery when eager-loading is set.
	Edges        TrackerEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TrackerEdges holds the relations/edges for other nodes in the graph.
type TrackerEdges struct {
	// Details holds the value of the details edge.
	Details []*Detail `json:"details,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DetailsOrErr returns the Details value or an error if the edge
// was not loaded in eager-loading.
func (e TrackerEdges) DetailsOrErr() ([]*Detail, error) {
	if e.loadedTypes[0] {
		return e.Details, nil
	}
	return nil, &NotLoadedError{edge: "details"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Tracker) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tracker.FieldID, tracker.FieldScore, tracker.FieldProgress, tracker.FieldTotalProgress, tracker.FieldStartDate, tracker.FieldFinishDate:
			values[i] = new(sql.NullInt64)
		case tracker.FieldTrackerID, tracker.FieldProvider, tracker.FieldStatus:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Tracker fields.
func (_m *Tracker) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tracker.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case tracker.FieldTrackerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tracker_id", values[i])
			} else if value.Valid {
				_m.TrackerID = value.String
			}
		case tracker.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = tracker.Provider(value.String)
			}
		case tracker.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case tracker.FieldScore:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field score", values[i])
			} else if value.Valid {
				_m.Score = new(int)
				*_m.Score = int(value.Int64)
			}
		case tracker.FieldProgress:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field progress", values[i])
			} else if value.Valid {
				_m.Progress = int(value.Int64)
			}
		case tracker.FieldTotalProgress:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_progress", values[i])
			} else if value.Valid {
				_m.TotalProgress = new(int)
				*_m.TotalProgress = int(value.Int64)
			}
		case tracker.FieldStartDate:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field start_date", values[i])
			} else if value.Valid {
				_m.StartDate = new(int64)
				*_m.StartDate = value.Int64
			}
		case tracker.FieldFinishDate:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field finish_date", values[i])
			} else if value.Valid {
				_m.FinishDate = new(int64)
				*_m.FinishDate = value.Int64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Tracker.
// This includes values selected through modifiers, order, etc.
func (_m *Tracker) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDetails queries the "details" edge of the Tracker entity.
func (_m *Tracker) QueryDetails() *DetailQuery {
	return NewTrackerClient(_m.config).QueryDetails(_m)
}

// Update returns a builder for updating this Tracker.
// Note that you need to call Tracker.Unwrap() before calling this method if this Tracker
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Tracker) Update() *TrackerUpdateOne {
	return NewTrackerClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Tracker entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Tracker) Unwrap() *Tracker {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Tracker is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Tracker) String() string {
	var builder strings.Builder
	builder.WriteString("Tracker(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tracker_id=")
	builder.WriteString(_m.TrackerID)
	builder.WriteString(", ")
	builder.WriteString("provider=")
	builder.WriteString(fmt.Sprintf("%v", _m.Provider))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	if v := _m.Score; v != nil {
		builder.WriteString("score=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("progress=")
	builder.WriteString(fmt.Sprintf("%v", _m.Progress))
	builder.WriteString(", ")
	if v := _m.TotalProgress; v != nil {
		builder.WriteString("total_progress=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.StartDate; v != nil {
		builder.WriteString("start_date=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.FinishDate; v != nil {
		builder.WriteString("finish_date=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteByte(')')
	return builder.String()
}

// Trackers is a parsable slice of Tracker.
type Trackers []*Tracker
